name: Release Obsidian plugin

# Triggered by pushing a semver tag like `0.1.0` (no `v` prefix — required
# by the Obsidian community plugin validator). Use `npm version <semver>`
# locally to bump + tag, then `git push --follow-tags`. The repo's .npmrc
# sets `tag-version-prefix=""` so `npm version` does not add `v`.
#
# Runs on macOS because the `sysrec` recording helper is a Swift/ScreenCaptureKit
# binary that only builds on macOS. The release attaches main.js / manifest.json /
# styles.css (for BRAT) plus the ad-hoc-signed `sysrec` binary, which the plugin's
# doctor panel can download with one click ("sysrec を取得").
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

jobs:
  release:
    runs-on: macos-latest
    permissions:
      contents: write
      id-token: write
      attestations: write

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: "20"
          cache: "npm"

      - run: npm ci

      - name: Build plugin
        env:
          # esbuild の出力先を ./build に固定（開発機の root 出力ではなく）。
          OUT_DIR: ./build
        run: npm run build

      - name: Build sysrec helper (Swift / ScreenCaptureKit)
        run: npm run build-sysrec

      - name: Verify build artifacts
        run: |
          test -f ./build/main.js
          test -f ./build/manifest.json
          test -f ./build/styles.css
          test -f ./native/sysrec/sysrec

      # Attestation はリリース成功の必須条件ではない（codeload の一時障害で
      # 失敗し得るため）。健康なら付く「ベストエフォート」運用にする。
      - name: Attest build provenance
        continue-on-error: true
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            ./build/main.js
            ./build/styles.css
            ./build/manifest.json
            ./native/sysrec/sysrec

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          gh release create "$tag" \
            --title="$tag" \
            --generate-notes \
            ./build/main.js \
            ./build/manifest.json \
            ./build/styles.css \
            ./native/sysrec/sysrec
